# Scriptum API v2.5 - Python Dependencies
# Web Framework
Flask==3.1.2
flask-cors==6.0.2

# Configuration
python-dotenv==1.2.1

# HTTP Requests
requests==2.31.0

# Cloud Storage (for large file uploads)
google-cloud-storage==2.18.2
google-cloud-firestore==2.19.0  # Job persistence across Cloud Run instances

# Subtitle Processing
pysrt==1.1.2
rarfile==4.2  # For extracting LegendasDivx RAR archives
langdetect==1.0.9  # Language detection for subtitles

# Video Processing (optional, for video analysis)
# ffmpeg-python==0.2.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-mock==3.14.0

# Optional: Rate Limiting (uncomment if implementing)
# Flask-Limiter==3.8.0

# Production Server (for Render deployment)
gunicorn==23.0.0
